import {defineMessages} from 'react-intl';

const messages = defineMessages({
    PageNotFound: {
        id: 'common.Error.PageNotFound',
        defaultMessage: '您要请求的页面不存在'
    },
});

export default messages;
